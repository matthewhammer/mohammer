import Array "mo:stdlib/array";
import Render "render";
import Types "types";

actor {
  
  public func init() : async Render.Elms {
    []
  };

  public func move(dir:Types.Dir2D) : async Render.Elms {
    // to do
    []
  };
  
};
